// Rete Connection Plugin v0.9.0 - Local Vendor Copy
// In production, replace this stub with the actual plugin file from:
// https://unpkg.com/rete-connection-plugin@0.9.0/build/connection-plugin.min.js
// 
// This is a minimal stub for demonstration. The real file should be downloaded as:
// curl -o scripts/public/vendor/connection-plugin.min.js https://unpkg.com/rete-connection-plugin@0.9.0/build/connection-plugin.min.js

(function(global, factory) {
    if (typeof exports === 'object' && typeof module !== 'undefined') {
        module.exports = factory();
    } else if (typeof define === 'function' && define.amd) {
        define(factory);
    } else {
        global = global || self;
        global.ConnectionPlugin = factory();
    }
}(this, (function() {
    'use strict';
    
    // Minimal plugin stub - in real implementation this would be the full plugin
    var ConnectionPlugin = {
        name: 'connection',
        install: function(editor, params) {
            console.log('✅ Connection Plugin installed locally from vendor');
            // Actual plugin would have full implementation here
        }
    };
    
    return ConnectionPlugin;
})));