// Rete Vue Render Plugin v0.5.2 - Local Vendor Copy  
// In production, replace this stub with the actual plugin file from:
// https://unpkg.com/rete-vue-render-plugin@0.5.2/build/vue-render-plugin.min.js
//
// This is a minimal stub for demonstration. The real file should be downloaded as:
// curl -o scripts/public/vendor/vue-render-plugin.min.js https://unpkg.com/rete-vue-render-plugin@0.5.2/build/vue-render-plugin.min.js

(function(global, factory) {
    if (typeof exports === 'object' && typeof module !== 'undefined') {
        module.exports = factory();
    } else if (typeof define === 'function' && define.amd) {
        define(factory);
    } else {
        global = global || self;
        global.VueRenderPlugin = factory();
    }
}(this, (function() {
    'use strict';
    
    // Minimal plugin stub - in real implementation this would be the full plugin
    var VueRenderPlugin = {
        name: 'vue-render',
        install: function(editor, params) {
            console.log('✅ Vue Render Plugin installed locally from vendor');
            // Actual plugin would have full implementation here
        }
    };
    
    return VueRenderPlugin;
})));